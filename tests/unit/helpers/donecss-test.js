import { donecss } from 'plan/helpers/donecss';
import { module, test } from 'qunit';

module('Unit | Helper | donecss');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = donecss([42]);
  assert.ok(result);
});
