import { getbucketlist } from 'plan/helpers/getbucketlist';
import { module, test } from 'qunit';

module('Unit | Helper | getbucketlist');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getbucketlist([42]);
  assert.ok(result);
});
